# Environment variables for DEGEN ROAST 3000
PORT=8000
HUGGINGFACE_API_TOKEN=your_huggingface_token_here
API_BASE_URL=https://api.example.com
# Add other environment variables as needed, but NEVER include real secrets

# Hugging Face API Token
# Get your token from: https://huggingface.co/settings/tokens
HF_TOKEN=your_huggingface_token_here

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# Optional: Server Port (defaults to 3000 if not specified)
# PORT=3000

# API Configuration
API_KEY=your_api_key_here

# Session Configuration
SESSION_SECRET=your_session_secret_here

# Optional Features
DEBUG=false
LOG_LEVEL=info

# Server Configuration
NODE_ENV=production

# Debug mode (set to false in production)
DEBUG_MODE=false

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual credentials
# 3. Never commit your actual .env file to version control

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# The application uses the DeepSeek-R1 model for enhanced roast generation
# You can verify your token with: node verify-token.js

# 3. Never commit your actual .env file to version control 